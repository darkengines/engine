#include <stdio.h>
#include <stdlib.h>
#include "Vector3D.h"
#include "System.h"

int main(int argc, char** argv) {
	bool done = 0;
	System* system = new System();
	system->Initialize();
	while (!done) {
		
	}
	getchar();
	return 0;
}